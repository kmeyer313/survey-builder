== README

If your environment can run rails, clone the repo, and then in the terminal run: bundle install, rake db:drop, rake db:create, rake db:migrate, rake db:seed. Run rails s to start the server, the port should be localhost:3000
